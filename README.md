# config

Minimal recovery repo for app preferences.

- Track only hand-edited config worth restoring quickly.
- Ignore app state, backups, generated files, and machine-specific noise.
- `dotfiles` bootstraps the system. `config` owns `~/.config`.
